# Deprecated: GitHub Actions only runs workflows from the repo root.
# Real CI is at .github/workflows/ci-programs.yml
name: deprecated
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated - see .github/workflows/ci-programs.yml"
