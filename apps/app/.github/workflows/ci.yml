# Deprecated: GitHub Actions only runs workflows from the repo root.
# Real CI is at .github/workflows/ci-app.yml
name: deprecated
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated - see .github/workflows/ci-app.yml"
