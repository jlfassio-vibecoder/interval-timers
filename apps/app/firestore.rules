rules_version = '2';
// App uses Supabase for data. Firestore is no longer used.
// These rules deny all access so no Firestore usage is possible.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
