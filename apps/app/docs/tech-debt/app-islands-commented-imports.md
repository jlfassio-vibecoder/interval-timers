# AppIslands: Commented-Out Imports

**File:** `src/components/react/AppIslands.tsx`  
**Status:** Intentional; components will be reused.

The following imports are commented out in `AppIslands.tsx` and are not used directly in the file:

- `AIChat`
- `WorkoutCards`
- `PurchaseFlow`
- `FluidBackground`

These are kept (commented) because the components are used elsewhere as islands or will be wired back in. Do not remove the commented import block without confirming they are no longer needed or have been moved.
