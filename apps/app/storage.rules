rules_version = '2';
// App uses Supabase Storage for uploads. Firebase Storage is no longer used.
// These rules deny all access so no Firebase Storage usage is possible.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
