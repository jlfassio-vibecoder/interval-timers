/**
 * Shared "Live session" title for Trainer Live host and client chrome (single source for label styling).
 */
export default function TrainerLiveSessionBrandingBar() {
  return (
    <h1 className="shrink-0 font-heading text-sm font-bold uppercase tracking-tight text-orange-light md:text-base">
      Live session
    </h1>
  );
}
